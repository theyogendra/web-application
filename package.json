{
  "name": "enterprise-platform",
  "version": "1.0.0",
  "description": "Enterprise Business Operating Platform",
  "scripts": {
    "dev": "echo 'Docker is failing. Use npm run local:dev to run without Docker.'",
    "local:dev": "npm run local:dev:backend & npm run local:dev:frontend",
    "local:dev:frontend": "cd frontend && npm run dev",
    "local:dev:backend": "cd backend && python -m uvicorn main:app --reload",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "author": "",
  "license": "ISC"
}
