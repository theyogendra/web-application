# Copy to frontend/.env.local
# Backend defaults to HTTPS on port 8000 (ENABLE_HTTPS=true, self-signed cert).
# If you started the backend with ENABLE_HTTPS=false, change this to http://.
NEXT_PUBLIC_API_URL=https://localhost:8000
